{
  "name": "prepo-hardhat",
  "version": "0.0.0",
  "description": "prePO Hardhat config",
  "main": "src/index.ts",
  "files": [
    "src/index.ts",
    "src/types.ts"
  ],
  "scripts": {
    "build": "tsc",
    "build:watch": "yarn build --watch"
  },
  "dependencies": {
    "@nomiclabs/hardhat-ethers": "yarn:hardhat-deploy-ethers@0.3.0-beta.13",
    "keccak256": "1.0.3",
    "merkletreejs": "0.2.24",
    "prepo-constants": "*"
  },
  "devDependencies": {
    "config": "*",
    "tsconfig": "*"
  }
}
