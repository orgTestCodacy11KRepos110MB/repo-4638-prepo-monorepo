{
  "name": "prepo-constants",
  "version": "0.0.0",
  "description": "prePO Constants",
  "main": "src/index.ts",
  "files": [
    "src/index.ts",
    "src/types.ts"
  ],
  "scripts": {
    "build": "tsc",
    "build:watch": "yarn build --watch"
  },
  "dependencies": {
    "@nomiclabs/hardhat-ethers": "yarn:hardhat-deploy-ethers@0.3.0-beta.13",
    "ethers": "5.6.9"
  },
  "devDependencies": {
    "config": "*",
    "tsconfig": "*"
  }
}
